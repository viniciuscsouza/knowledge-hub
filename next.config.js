/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: Descomente a linha abaixo se as imagens do Google não carregarem após o deploy
  // images: { unoptimized: true },
};

module.exports = nextConfig;
